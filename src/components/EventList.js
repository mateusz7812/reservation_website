import React from "react";
import Event from "./Event";

const EventList = (eventList)=>{
    return(
        <div>
            {
                //eventList.forEach(item=>{
                //    return <Event name={item["name"]} startDate={item["startDate"]}/>;
                //})
            }
        </div>
    )
};

export default EventList;